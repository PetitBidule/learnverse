import 'package:flutter/material.dart';

class ScreenLog extends StatelessWidget {
  const ScreenLog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("sefh")),
    );
  }
}
